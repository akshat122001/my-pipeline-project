name: CI/CD Pipeline
on:
  push:
    branches: [ develop ]

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: mkdir cloned_content
    - run: xcopy /E /I * cloned_content\
    - run: dir cloned_content 
